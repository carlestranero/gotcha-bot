{
  "name": "gotcha-bot",
  "version": "1.0.0",
  "description": "Gotcha — a Discord bot that turns any message into a quote image, with a 📌 pin-bypass to a dedicated channel",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "deploy": "node deploy-commands.js"
  },
  "engines": { "node": ">=22.12.0" },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.52",
    "discord.js": "^14.26.4",
    "dotenv": "^16.4.5",
    "twemoji-parser": "^14.0.0"
  }
}
